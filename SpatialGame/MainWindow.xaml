<Window x:Class="SpatialGame.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ht="http://helix-toolkit.org/wpf/SharpDX"
        xmlns:local="clr-namespace:SpatialGame" 
        xmlns:viewmodels="clr-namespace:SpatialGame.ViewModels" 
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d"
        Title="SpatialGame" Height="600" Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="4">
            <!-- Buttons / outputs will go here -->
        </StackPanel>

        <ht:Viewport3DX
            x:Name="View"
            Grid.Row="1"
            Background="Black"
            ShowCoordinateSystem="True"
            ZoomExtentsWhenLoaded="True"
            EffectsManager="{Binding EffectsManager}"
            Margin="4">
            <ht:Viewport3DX.Camera>
                <ht:PerspectiveCamera Position="0,0,40" LookDirection="0,0,-4" UpDirection="0,1,0" FieldOfView="45" />
            </ht:Viewport3DX.Camera>
            <ht:AmbientLight3D Color="#FFFFFF" />
            <ht:DirectionalLight3D Color="#FFFFFF" Direction="-1,-1,-1" />
            <ht:DirectionalLight3D Color="#FFFFFF" Direction="1,1,0.5" />
            <ht:MeshGeometryModel3D
                x:Name="Model"
                Visibility="Visible"
                IsEnabled="True"
                RenderOptions.EdgeMode="Aliased"
                IsThrowingShadow="True"
                RenderWireframe="False"
                WireframeColor="Yellow"
                CullMode="None"
                Geometry="{Binding LatticeMesh}"
                Material="{Binding MeshMaterial}"
                Transform="{Binding MeshTransform}">
            </ht:MeshGeometryModel3D>
        </ht:Viewport3DX>
    </Grid>
</Window>
