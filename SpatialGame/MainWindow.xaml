<Window x:Class="SpatialGame.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ht="http://helix-toolkit.org/wpf/SharpDX"
        xmlns:local="clr-namespace:SpatialGame" 
        xmlns:viewmodels="clr-namespace:SpatialGame.ViewModels" 
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d"
        Title="SpatialGame" Width="1024" Height="768" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="4">
            <!-- Buttons / outputs will go here -->
        </StackPanel>

        <ht:Viewport3DX
            x:Name="UniverseView"
            Grid.Row="1"
            Background="Black"
            ShowCoordinateSystem="True"
            ZoomExtentsWhenLoaded="False"
            EffectsManager="{Binding EffectsManager}"
            Margin="4">
            <ht:Viewport3DX.Camera>
                <ht:PerspectiveCamera Position="0,0,80" LookDirection="0,0,-80" UpDirection="0,1,0" FieldOfView="45" />
            </ht:Viewport3DX.Camera>

            <!-- Explicit lights so the sun actually lights the scene 
            <ht:AmbientLight3D Color="#00ffffff" />-->

            <ht:PointLight3D Color="White" Position="0,0,0" Range="100" Attenuation="1,0,0"/>


        </ht:Viewport3DX>
    </Grid>
</Window>
